{% extends "index.html.twig" %}

{% block content_main %}
  <ul class="inline">
    {% for post in posts %}
      <li>
        <a href="{{ relativeRoot }}/{{ post.path }}">{{ post.title|title }}</a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}
